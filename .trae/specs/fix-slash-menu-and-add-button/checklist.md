- [x] `AddBlockButton.tsx` 中 `onClick` 已从 icon 移到 `Components.SideMenu.Button` 的 prop 上
- [x] `LcwDocDefaultUI.tsx` 中已添加 `{props.children}` 渲染
- [x] `LcwDocView.tsx` 中 context 值已包含 `setContentEditableProps`
- [x] `SuggestionMenuItem.tsx` 中 `querySelector('.bn:suggestion-menu')` 已修复为 `querySelector('.bn-suggestion-menu')`
- [x] `GridSuggestionMenuItem.tsx` 中 `querySelector('.bn:grid-suggestion-menu')` 已修复为 `querySelector('.bn-grid-suggestion-menu')`
- [x] `packages/react` 已重新构建，构建产物包含修复后的代码
- [x] `packages/shadcn` 已重新构建，构建产物包含修复后的代码
- [x] 在编辑器中输入 "/" 斜杠菜单正确弹出
- [x] 斜杠菜单包含默认块类型（标题、列表等）和自定义 AI 选项
- [x] 斜杠菜单搜索过滤功能正常
