# Tasks

- [x] Task 1: 在 package.json 中添加 deploy 相关脚本
  - [x] 添加 `deploy` 脚本：git push origin main && npm version patch && git push origin main --tags
  - [x] 添加 `deploy:minor` 脚本：git push origin main && npm version minor && git push origin main --tags
  - [x] 添加 `deploy:major` 脚本：git push origin main && npm version major && git push origin main --tags

# Task Dependencies
（无依赖，单一任务）
