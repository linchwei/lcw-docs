- [x] `useDocOutline` hook 能正确从编辑器提取标题列表（id、level、文本）
- [x] `useDocOutline` hook 在编辑器内容变化时实时更新标题列表
- [x] `useDocOutline` hook 在光标位置变化时返回当前高亮标题的 id
- [x] `DocOutline` 组件按层级缩进显示标题（h1 无缩进，h2 一级，h3 两级）
- [x] `DocOutline` 组件点击标题项能跳转到编辑器对应位置
- [x] `DocOutline` 组件当前标题项显示高亮样式
- [x] `DocOutline` 组件在无标题时隐藏
- [x] `Doc/index.tsx` 布局正确：编辑区域居中 + 右侧目录面板
- [x] 编辑器中新增标题后目录实时更新
- [x] 编辑器中删除标题后目录实时更新
- [x] 编辑器中修改标题文本后目录实时更新
